#include <stdio.h>
#include <math.h>

void itob(int n, char s[], unsigned short b);

int main()
{
	
}

void itob(int n, char s[], unsigned short b)
{
	int i, token, res;

	s = {'\0'};
	switch(b)
	{
		base = 0;
		token = n/b;
		s[0] = (n % b) >= 10 ? '0' : 'A';
		for(i = token-1; i > 0; i--)
			s[i] = strcat(pow(b, i))
	}
}
